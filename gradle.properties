# Spring Boot 2.7.x line (downgraded from the 4.1.0 upgrade). springCloudVersion pinned to
# 2021.0.9 (train "Jubilee"), the last Spring Cloud release train compatible with Boot 2.7.x
# and the last to ship spring-cloud-starter-sleuth / spring-cloud-contract 3.1.x. springDocVersion
# pinned to the 1.x line (javax-based, matches springdoc-openapi-ui, not the 3.x starter).

springBootVersion=2.7.18
springCloudVersion=2021.0.9
springDocVersion=1.8.0
spotlessVersion=6.25.0
sonarqubeVersion=7.4.0.8496
testcontainersVersion=1.19.8

org.gradle.jvmargs=-Xmx1536m
